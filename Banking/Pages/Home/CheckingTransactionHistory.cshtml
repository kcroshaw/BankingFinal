@page
@model Banking.Pages.Home.CheckingTransactionHistoryModel
@{
}
<!DOCTYPE html>
<html>
    <head>
        <style>
              .tableFixHead {
                overflow-y: auto;
                height: 500px;
                width: 75%;
                margin-right: auto;
                margin-left: auto;
              }
              .tableFixHead thead th {
                position: sticky;
                top: 0;
              }
              table {
                border-collapse: collapse;
                width: 100%;
              }
              th, td {
                padding: 8px 16px;
                border: 1px solid #ccc;
              }
              th {
                background: #eee;
              }
        </style>
    </head>

    <body>
    <h1 class="display-4 text-center">Checking Transaction History</h1>
        <div class="tableFixHead">
            <table>
                <thead>
                    <tr>
                        <th>Date</th>
                        <th>Transaction Type</th>
                        <th>Account</th>
                        <th>Amount</th></>
                    </tr>
                    <td>@Model.v)</td>
                </thead>
                <tbody>
                    @*@foreach (var item in ?????){
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.TransactionDate)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.TransactionType)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.TransactionAmount)
                            </td>
                        </tr>
                     }*@
                </tbody>
            </table>
        </div>
        <input type="button" class="centered" value="Back" onclick="@("window.location.href='" + @Url.Action("Dashboard", "Home") + "'");" />
    </body>
</html>